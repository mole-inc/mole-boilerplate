name: Build

on:
  push:
    branches:
      - main

jobs:
  build:
    runs-on: ubuntu-lates
    name: tsc
    steps:
      - name: checkout
        uses: actions/checkout@v2
      - name: install
        uses: npm ci
      - name: build
        uses: npm run build
